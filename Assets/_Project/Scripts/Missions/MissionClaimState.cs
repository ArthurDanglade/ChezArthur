namespace ChezArthur.Missions
{
    /// <summary>
    /// État runtime / save d'une mission (aligné SaveData.state).
    /// </summary>
    public enum MissionClaimState
    {
        InProgress = 0,
        Completed = 1,
        Claimed = 2
    }
}
